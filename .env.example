# Copy to `.env` and fill in. Both keys come from Google Cloud Console:
#   APIs & Services → Credentials → Create credentials → OAuth client ID
#   Application type: Desktop app
# Download the JSON; copy `client_id` and `client_secret` here.
#
# `.env` is git-ignored. Never commit real credentials.

GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
